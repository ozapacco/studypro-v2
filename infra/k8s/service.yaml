apiVersion: v1
kind: Service
metadata:
  name: studypro-api
  labels:
    app: studypro-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: studypro-api
